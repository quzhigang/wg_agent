#针对我们所有未提交的更改创建一个新的 commit。
-- 运行 git status && git diff HEAD && git status --porcelain 查看哪些文件尚未提交。
-- 添加未跟踪及已更改的文件。
-- 按照前面的提交格式，如第N次提交，后面添加一条简洁明了的原子 commit 消息。
-- 添加一条简洁明了的原子 commit 消息。
-- 添加反映我们工作内容的标签，例如 "feat"、"fix"、"docs" 等。
